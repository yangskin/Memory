{
  "_comment": "Copy this file to user_config.local.json. It is gitignored. user_name is also sent to Memory Hub as user_id when a shared project token is used.",
  "user_name": "your-name",
  "shared_memory": {
    "enabled": false,
    "server_url": "https://memory.example.com",
    "project_id": "your-project-id",
    "token": "mem_v1.<token-id>.<secret>"
  }
}
