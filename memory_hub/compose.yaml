services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: memory_hub
      POSTGRES_USER: memory_hub
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
    volumes: [postgres_data:/var/lib/postgresql/data]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U memory_hub -d memory_hub"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
  api:
    build:
      context: .
      args:
        PIP_INDEX_URL: https://mirrors.cloud.tencent.com/pypi/simple
    environment:
      MEMORY_HUB_DATABASE_URL: postgresql+psycopg://memory_hub:${POSTGRES_PASSWORD}@postgres/memory_hub
      MEMORY_HUB_DISABLE_DOCS: "true"
      MEMORY_HUB_PUBLIC_BASE_URL: https://${MEMORY_HUB_PUBLIC_HOST:?set MEMORY_HUB_PUBLIC_HOST}
      MEMORY_HUB_WEB_DIR: /app
      MEMORY_HUB_ENV: production
    command: sh -c "alembic upgrade head && memory-hub-api"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8000/healthz', timeout=3)"]
      interval: 15s
      timeout: 5s
      retries: 5
    restart: unless-stopped
  caddy:
    image: caddy:2.8-alpine
    ports: ["80:80", "443:443"]
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./certs:/etc/caddy/certs:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      MEMORY_HUB_PUBLIC_HOST: ${MEMORY_HUB_PUBLIC_HOST:?set MEMORY_HUB_PUBLIC_HOST}
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
  worker:
    build:
      context: .
      args:
        PIP_INDEX_URL: https://mirrors.cloud.tencent.com/pypi/simple
    environment:
      MEMORY_HUB_DATABASE_URL: postgresql+psycopg://memory_hub:${POSTGRES_PASSWORD}@postgres/memory_hub
      LLM_PROVIDER: ${LLM_PROVIDER:-fake}
      LLM_BASE_URL: ${LLM_BASE_URL:-}
      LLM_API_KEY: ${LLM_API_KEY:-}
      LLM_MODEL: ${LLM_MODEL:-}
      LLM_TIMEOUT_SECONDS: ${LLM_TIMEOUT_SECONDS:-60}
      BRIEF_USER_DEBOUNCE_SECONDS: ${BRIEF_USER_DEBOUNCE_SECONDS:-20}
      BRIEF_PROJECT_DEBOUNCE_SECONDS: ${BRIEF_PROJECT_DEBOUNCE_SECONDS:-45}
      BRIEF_REBASE_INTERVAL_SECONDS: ${BRIEF_REBASE_INTERVAL_SECONDS:-3600}
      PROJECT_GRAPH_SEMANTIC_ENABLED: ${PROJECT_GRAPH_SEMANTIC_ENABLED:-false}
    command: memory-hub-worker
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
volumes:
  postgres_data:
  caddy_data:
  caddy_config: