# Memory 存储、索引与多端同步调整方案

状态：实现与最终全量验证完成；历史文件路径退役保持关闭，发布回读与任务记忆收尾见交付记录。本文是本次调整的唯一实施与验收入口，通用架构仍见 MemorySystemDesignDocument.md。

## 1. 目标与边界

目标：减少启动后首次检索耗时、消除每任务小文件的持续增长、减少多 PC 同用户提交冲突、精简空元数据并修正自动标签污染。Markdown 保持唯一可审计、可重建真源；SQLite/使用统计为本机派生物，不提交。LLM 仍在确定性检索后生成派生总结，不参与数据库灌入或 Git 合并。

本次不改 Hub 身份/权限、Task Graph、UE 能力、模型服务/凭据或公开 MCP 工具签名，不新增网络依赖或在线协调服务。不依赖远端在线才能写记忆。延迟目标为部署预建后当前数据规模的完整确定性首次检索 <10 秒；默认交互式 LLM 增强与本地简报共用 8 秒等待预算，给协议与输出留余量；超时明确标记并返回已检索的确定性证据，不省略历史检索。可用 llm_defaults.capabilities.generate_task_brief.interactive_budget_seconds 显式调整为 (0,120] 秒，延长配置不承诺 10 秒。真实服务的完成/超时分别报告。

## 2. 已定位问题

1. 已有局部索引更新，但更新后仍全库记录快照；查询再次全库校验，SQLite 仅返回路径，随后重新读/解析大量 Markdown；简报末尾再次计算全库水位。
2. 普通写入按 task_id/branch 分包，任务结束只写一次时形成一文件一记录；历史归档按单一来源生成不可变片，无法减少源文件数量。
3. v2 写入总是输出全部可选元数据，空字段和重复默认信息长于许多正文。
4. 自动共享摘要无条件追加 mcp 主题标签，降低标签的区分度。
5. 部署仅准备环境/工具注册，SQLite 首次构建留到用户首次查询。
6. 实际项目启用 Hub 时，task_context 还同步等待 Board 查询和共享上下文刷新；TLS/DNS 初始化不完全受 socket timeout 约束，形成独立于记忆索引的等待。

## 3. 格式与兼容合同

### 3.1 第一阶段坚持可互读格式

保留 version=1 的 Markdown pack 边界、v1/v2 记录 schema、每条记录 id/record_kind/author/scope/status/created_at 等旧消费者必需信息。新写入仅省略已定义且无值的可选字段，保留未知字段、false、0 和非空关系。作者和范围暂不只放文件头：这样旧客户端无需理解新的文件级继承也能正确判断权限。这是优先兼容性的明确设计选择，不为节省少量重复字节引入危险的新格式。

新读端读取旧完整头和新稀疏头；默认值规范化只用于已定义语义，不能把显式清除指令等同缺省。导出/合并比较按逻辑记录，不按 YAML 排版。未知更高 schema 不做破坏性迁移，保留数据并给出可诊断错误。

### 3.2 原始记录与历史可变流程

普通新记忆只追加；更正通过新记录与 supersedes。历史治理/lineage API 保持兼容，不擅自删除已发布/已验证记录的更新能力。只有同 ID、同语义的重复可以去重，同 ID 不同正文或元数据不能以时间戳自动覆盖。

### 3.3 标签

保持受控词表和现有查询语义；取消共享摘要自动添加 mcp。标签仅表示其实际语义，生成来源使用 provenance/derived_from_record_ids。历史 mcp 标签不全量盲删；只在能够证明来源传播关系时提供可审计纠正路径。读者仍可使用旧标签，查询与共享判断不因格式精简失效。

## 4. 周日志与多个克隆

逻辑时间线按用户与范围合并；物理分包按用户/范围/ISO UTC 周/长期克隆副本/分卷。不同 PC/独立克隆使用本地持久随机副本 ID；ID 不使用硬件指纹，不随 Git 同步。相同克隆的临时 worktree 共用长期身份，不按 agent/run/task 创建新桶；任务/Agent 来源保留在记录信息中。

新日志使用 `memory-bank/archive/record-packs/journal/`，该容器已被旧版归档器和重打包器跳过，避免旧客户端搬走活跃周日志；记录是否活跃和可见范围仍由每条 metadata 决定，不由目录名推断。不重构现有历史目录。只有发生写入才创建文件，同一时间窗尽量填满，超出当前配置容量后本副本分卷。合并导致文件临时变大时，合并驱动不能跨文件做拆分；后续写入另开分卷。新周日志不得由日常旧归档流程搬走或删除，避免离线 PC 补交与删除/重命名冲突。

同目录写入复用已有 OS 文件锁与原子发布。跨克隆通常写不同文件，由 Git 普通合并；同一副本不同分支同文件分叉由记录级三方驱动处理。驱动只作用 Memory pack：校验三个版本、保留两端独立新增、相同 ID 同内容去重、确定顺序输出；对同 ID 内容变化、删除/修改冲突、损坏边界明确失败，不静默丢失原始记录。不使用逐行 union。

部署在本地 Git config 注册驱动，并在版本化 attributes 中提供范围明确的规则。普通 clone 不复制本地配置，必须在每个克隆部署并自测。Git 合并仍由用户/Agent 现有流程触发，本工具不自动提交或推送其他变更。源代码 subtree 发布与项目私有 memory-bank 数据严格分离。

## 5. SQLite 完整增量读取

SQLite 保存完整解析元数据、正文、路径和打包标识；FTS/关系/权限查询以这些字段构造完整逻辑记录，不再依赖候选 Markdown 重读。旧数据库进行可重建迁移，已知不兼容派生 schema 重新准备，不改 Markdown。

维护源文件路径与内容签名，严格发现外部改写后只解析/替换新增与变化文件，删除文件只删除对应行。不能仅信任 mtime+size：保留时间戳的等长改写必须被发现。本轮保留一次严格内容发现；不会用 TTL 暗中牺牲新鲜度。正常 MCP 写入仅更新已知路径的记录与源签名，不做全库重建。跨请求/跨进程竞争需要事务与已有本地锁保证，变化检测与内容解析不一致必须重试或报错。

查询的语料水位复用本次确认后的索引水位，避免简报尾部二次全库校验。MCP 最外层裁剪优先保留 context_token、task_brief、generation 与 provenance，防止有界响应把整个简报丢掉。LLM 晚到结果不写缓存；每个进程最多两个尚未结束的摘要请求，不做隐藏网络重试；耗尽等待预算后保留明确的 timeout/busy 状态与确定性证据。查询仍执行原有用户可见性、标签、facet、有效时间、关系折叠和排序规则。索引损坏或外部变化持续发生不能伪装正常命中；返回可诊断的恢复/失败状态。

自动注入的 Board 与共享上下文属于咨询信息：先返回本地快照，显式标记 fresh/stale/pending 和来源。Board 使用按服务、凭据摘要、项目和查询隔离的缓存，后台最多两个查询，同键合并并发；成功空结果会替换旧快照，避免持续显示已解决项，失败有诊断。共享上下文沿用既有同步 worker 更新。专用主动查询和 Task Graph 的 Hub 权威判定保持原样；该优化不能把共享任务状态转换变成本地决定。

## 6. 部署与历史迁移

部署在环境就绪后运行本地 prepare：校验配置/数据格式，注册 Git 集成，准备/增量刷新 SQLite，完整性检查和查询冒烟后才报告准备完成。标准 deploy、bootstrap 及旧包装入口覆盖到；多进程部署避免重复同时重建。

历史归并通过显式管理命令进行，默认计划/演练。按用户/范围/月份跨来源聚合冷 pack，保留每条正文、ID、元数据、来源和关系；容量受控，不再每源文件一个归档片。生成包含源内容签名、目标、计数、完整逻辑摘要的清单；先写入目标、校验等价，再删除清单内仍与原签名一致的来源；失败保留原数据。支持幂等恢复、旧 ID/路径定位和明确回滚备份。未知格式/同 ID 内容冲突不迁移。

实际项目数据先复制到隔离目录，执行迁移和数据库重建、检索对比，确认无丢失且所有读取端已支持迁移清单后才可应用。默认 `--apply` 也拒绝删除旧路径，必须额外显式声明 `--retire-legacy-paths`。活动周日志和可能仍追加的最新任务包不参与冷迁移；不让每台 PC 部署时各自重排历史。旧客户端仍可能在线时，本轮只发布兼容代码和本地索引，不退役项目历史路径。将来的 Git 数据迁移是项目侧单独、可审阅的提交，不进入 Memory 公共 subtree。

## 7. 实施任务

- [x] 读取记忆恢复上下文，确认源码边界与现有基线。
- [x] 形成本文、兼容矩阵、验证与回滚要求。
- [x] 稀疏写入、标签修正、解析/往返兼容测试。
- [x] 周日志副本身份、分卷、日常归档保护和并发测试。
- [x] 记录级 Git 合并与部署注册，多克隆/新旧格式演练。
- [x] SQLite 完整投影、增量刷新与复用水位，外部编辑/损坏/并发恢复测试。
- [x] 部署预建与标准/旧入口回归。
- [x] 历史冷包归并计划、事务/幂等/回滚和真实数据副本演练。
- [x] 全量 Python、PowerShell、Hub 兼容测试；外部依赖跳过项见 §10.5。
- [x] 当前数据首次确定性检索和冷部署基准；独立列明 LLM 耗时。
- [x] 独立检索质量验收：同一查询集在无 LLM、有 LLM、超时/无效 LLM 下检查预期 ID、相关性、替代/过期关系和用户隔离，对比基线召回与延迟。
- [x] 精确公共树安全检查并同步本文验收证据。

交付收尾顺序：只提交并发布已验证的 Memory 源码树，回读远端 commit/tree，随后写入记忆更新进度。具体发布 SHA 和结构化记忆写入结果以交付记录为准，避免在提交本身写入尚未发生的结果。

## 8. 必测矩阵

| 维度 | 验收内容 |
|---|---|
| 格式 | 旧 standalone/旧 pack/v1/v2、新稀疏 pack、缺省/null/空列表/false/0、未知字段、损坏边界 |
| 权限 | 用户、范围、任务可见性与迁移前一致，不从文件名猜权限 |
| 并发 | 同目录多进程、多克隆、临时 worktree、同路径 add/add、独立追加、同 ID 冲突 |
| Git | 幂等、互换分支顺序、重复提交、离线跨周、分卷与删除/修改不丢记录 |
| 索引 | 首次构建、旧 schema、只改一文件、删除/改名、保留 mtime 等长改写、损坏/竞争、FTS 与关系一致 |
| 部署 | 全新无 DB、已有 DB、可重入、失败不报告 ready、旧脚本与 worktree 定位 |
| 迁移 | 记录 ID/正文/关系计数与摘要相同、同 ID 冲突拒绝、途中失败、重跑、回滚 |
| 回归 | 全部既有测试及新增测试，公共树不可包含本机凭据/私有数据 |
| 质量 | 确定性标注查询集的 Recall@K/MRR/负例排除，与修改前基线对比；LLM 可用、禁用、超时、错误输出均不得造成权限越界或虚构记忆引用 |

速度与准确性是共同门禁。LLM 的可用性不能决定基础事实是否能被检索；其总结与查询改写只能在明确证据范围内工作。真实配置可用时补充真实服务冒烟，隔离测试使用可复现的确定性 provider；报告须区分真实服务测量与模拟，不用模拟耗时冒充实际性能。

## 9. 回滚与风险

修改前保留文件级备份和 diff。格式维持旧 parser 可读，代码回滚不要求数据反向转换；派生 DB 允许删除后重建。新周日志放在旧维护器已跳过的归档包子目录，确保上一版常规归档/重打包不会移动它；未知更早版本或第三方维护器仍需独立验证，不宣称任意历史客户端完全兼容。

历史迁移以清单和 `.ai-memory/pack-migration-originals/<source_sha256>.bak.md` 原文件备份恢复；不能用整库 reset 覆盖同期新增记忆。远端同步、LLM 延迟、真实多 PC 离线周期不可由单机测试完全证明，最终报告区分模拟与实际验收。

## 10. 当前真实数据演练证据

完整语料 12,865 个逻辑 ID，迁移前后正文与规范化元数据 SHA-256 一致。3,653 个冷归档来源合并为 67 个目标，参与迁移的 4,037 条逻辑记录不变，归档字节 11,560,220 → 9,231,559。记忆目录 Markdown 文件数 4,133 → 547。已在隔离副本完成应用、中断恢复、完整回滚、重新应用；8 组真实领域查询前 10 项返回结果及顺序全部一致。当前新代码迁移前查询 6.80–8.16 秒，迁移后 3.13–4.82 秒；完整确定性新任务读取 4.54/4.00 秒。时间为本机测量，不是任意硬件 SLA。

补充验收发现并修复：Windows 临时文件名重复完整目标名称造成路径超长；迁移原件备份改为内容寻址，避免叠加原目录。真实 MCP 大诊断响应曾被末层预算裁剪掉 task_brief，现将简报和引用列入保留优先级。同步真实 LLM 原耗时导致单次读取 11.44 秒，交互式增强已加入上述总等待预算。最终测试与真实服务状态见下列验收记录；发布 SHA 以远端回读为准。

### 10.1 混合旧客户端的发布门禁

以本次开始时的远端上一版代码运行真实副本：能够重建全部 12,865 个逻辑记录，8 组领域查询结果与新版本完全相同；但旧路径引用在文件被删除后返回 not_found。故“旧端能解析新 pack”不足以证明整体向后兼容。本轮生产项目保留原文件，不应用历史路径退役。新周日志、稀疏元数据和新/旧 SQLite 混用分别验证；日常部署不自动迁移历史。管理员只有确认所有读端支持迁移映射后才能显式通过退役门禁，不能依赖不完整的在线客户端清单或猜测离线 PC 已升级。


### 10.2 保留历史路径的实际规模首次读取

保留全部历史文件的独立副本包含 12,865 个逻辑记录、4,138 个 Markdown 文件（包含非原始记录的派生文档，和前述归并演练取样时点不同）。全新无 SQLite 的 prepare 用时 28.499 秒。这是首次部署成本；数据库不进入 Git，其他机器也在部署时各自构建。

| 模式 | 首次 memory_read | 进程启动/握手 + 首次读取 | 结果 |
|---|---:|---:|---|
| 无 LLM，两次独立进程 | 5.099 / 5.773 秒 | 7.046 / 8.190 秒 | 确定性简报、经验正文及引用均保留 |
| 真实 LLM，默认 8 秒预算 | 8.158 秒 | 10.275 秒 | 明确 timeout，返回已检索确定性证据 |
| 真实 LLM，临时显式 30 秒预算 | 14.276 秒 | 16.733 秒 | 一次模型调用成功，总结有实际证据引用 |

三种路径返回同一组 17 个基础引用 ID。公开发布副本没有本机密钥；真实服务只通过测试子进程环境读取现有配置，未写入公开树。输出裁剪保留任务简报和经验栏目，并明确 response_truncated；较长证据按引用继续读取。10 秒目标针对已 prepare 的单次读取，不承诺冷建库、模型必然完成、进程启动加读取或重度磁盘负载都在 10 秒内。

另一次 cProfile 检查用于定位，不能混作无插桩延迟：全程 16.985 秒，其中一次严格源快照 12.986 秒（包含路径枚举 8.512 秒），SQLite 完整性检查 0.468 秒。该次与四组测试并行且带分析开销；瓶颈仍是数千文件的路径/读取校验，并非每次重新解析全部 Markdown。日常写入不做全库发现；新日志减少未来文件增长，历史文件等所有读端升级后再归并。

### 10.3 旧 SQLite 写端交错验证

真实上一版源码和新源码分进程共用一个临时工作区，验证新端写入、新端预建、旧端重建/检索/按路径读取、旧端归档与重打包、旧端写入、新端继续写入再检索，共 5 条记录全部保留。专项发现过“新端写入覆盖旧端水位，但完整投影未修复”的缺陷，已加入写前及 SQLite 写事务内水位复核；读取事务也验证投影水位，旧端并发变化时返回可诊断重试要求。3 条专项回归覆盖连续混写、读取校验后的旧端提交、解析文件期间的旧端提交。


### 10.4 当前项目与原有配置的最终端到端读取

完整工作区曾测得无 LLM 首次读取 21.570 秒；分段定位显示同步 Board 查询约 4.710 秒，已取证的本地检索与简报约 7.707 秒，共享上下文冷刷新也会增加网络初始化耗时。前述隔离副本基准不能代替这一项。

最终实现保留原始记录全量校验，将咨询信息的自动注入改为明确标注新鲜度的缓存和后台刷新。原有项目配置保持启用，用真实 stdio 新进程、既有任务上下文并强制刷新本地简报测量：

| 当前工作区模式 | memory_read | 启动/握手 + 读取 | 验证结果 |
|---|---:|---:|---|
| 关闭 LLM | 3.815 秒 | 5.303 秒 | 完整检索后返回有界经验简报 |
| 启用真实 LLM，默认预算 | 8.132 秒 | 9.956 秒 | 明确 timeout，确定性证据仍返回 |

两次结果的 6 个基础引用 ID 完全一致；Board 返回实际缓存新鲜度，远端查询未完成时不能据此断言没有未决协作事项。上述是本机时点实测，不能保证任意硬件/负载的硬 SLA；无数据库时仍先完成部署预建。真实模型成功总结另有 30 秒预算下 14.276 秒的实测，不能宣称模型必然在 10 秒内生成。

专项补充：非 Git 工作区写入不再每条都启动 Git 再等待失败，仍检查祖先仓库并保留真实 clone/worktree 的 Git common-dir 身份；临时原子文件使用短随机名，测试继续校验同目录发布、O_EXCL、防截断和失败清理。


### 10.5 最终全量验证与可复现命令

最终候选共收集 1,235 项本地 Python 测试；按明确节点列表分成四个独立进程，核对收集数量、每组 JUnit 数量、全局唯一性与退出码，最终 1,229 通过、6 跳过、0 失败。跳过项为隔离测试环境未配置真实模型凭据的两项、未安装可选 onnxruntime 的三项、Windows 当前权限无法创建符号链接的一项。真实模型另用项目已有配置完成 §10.2/10.4 的成功和超时验证。

Hub 独立环境 39 通过、42 跳过、0 失败；42 项均需要 PostgreSQL，本机未具备该服务，不能视为已验证数据库集成。PowerShell 多工作区部署定位回归、Python compileall、精确公共候选树的文件/内容及 vendor 哈希审计均通过。最终测试后的改动限于说明文档，发布门禁会比较 Git 内容树，禁止夹带未测试的运行代码或测试改动。

8 组带预期记录 ID 的确定性查询全部 Top1 命中，故该有界测试集的 Recall@1 与 MRR 均为 1.0；SQL 完整投影、原路径读取与迁移后 Top5 ID 及顺序一致。五种模型模式（关闭、成功、超时、无效响应、未知引用）保持相同合法基础证据，并排除其他用户和过期事实。另有 8 组真实数据查询 Top10 顺序一致；这些验证不能等同任意自然语言问题的完美召回。

在 Memory 源码根目录、按 requirements-dev.txt 安装测试依赖后执行：

```powershell
python -m pytest tests/memory_server -q
python -m compileall -q servers scripts
python scripts/check_public_tree.py
& ./tests/setup_mcp_worktree_root.Tests.ps1
```

Hub 使用独立的 memory_hub 开发环境执行其测试；不向本地 MCP 环境安装 Hub 依赖。每个真实项目克隆升级后运行以下部署准备（或标准 deploy 入口），再重启已有 MCP 进程：

```powershell
python scripts/prepare_memory.py --root <项目克隆根目录>
```

当前真实项目再次核对 3,653 个历史来源：全部仍在原路径，SHA-256 无变化，未生成已应用迁移清单。Git 驱动配置不随 clone 同步，未部署的旧克隆仍可能产生普通文本冲突；不能以逐行 union 自动消除。已验证上一版客户端的读取、归档、重打包与新旧交替写入；更早未知版本仍需单独验证。

### 10.6 重启后的在线验收与副本定位修复

真实客户端重启后的 task_context 首次读取 5.844 秒；相同 task_brief 查询关闭 LLM 为 5.465 秒，启用真实 LLM 为 8.100 秒并明确报告 timeout，两者 10 个基础引用 ID 和顺序完全相同。精确历史提交检索 5.425 秒、Top1 正确。SQLite 为 12,957 个逻辑记录、4,180 个源文件，FTS 数量与投影水位一致，quick_check 通过。此次未运行重建来掩盖实际服务状态；检索没有重新解析候选 Markdown，但仍做严格源快照校验。

在线写入暴露了既有测试未覆盖的宿主进程差异：副本目录定位同步调用 Git，在该 MCP 宿主下返回 replica_identity_failed（Git common directory discovery timed out）。实测 Git 包装进程退出后子进程仍存活，调用可超过 subprocess 的 10 秒预算甚至触发客户端 90 秒超时，失败记录未落盘。普通 shell 中相同命令成功，因此不能把所有延迟归因于记忆文件规模，也不能仅凭隔离写入测试确认在线服务写入正常。

修复仅改变副本目录定位：直接解析 `.git` 目录或 gitdir 指针、worktree 的 commondir，保留 GIT_DIR/GIT_COMMON_DIR 的显式覆盖以及祖先发现边界；不再为写入启动 Git 进程。规则来自 [Git 仓库布局](https://git-scm.com/docs/gitrepository-layout)与 [Git 环境变量](https://git-scm.com/docs/git)。既有 memory-replica-id 原位复用；缺失目标、损坏指针或损坏身份明确失败，不能转移到另一个目录生成新身份。没有改变日志格式、索引协议或历史文件。

验证新增真实 Git 仓库写入且禁止启动子进程、独立 gitdir、显式环境路径、损坏元数据不产生替代身份；原有真实 clone/worktree 测试在 Git 建仓完成后同样禁止启动 Git。全量测试与发布结果另记交付证据。当前已运行的 MCP 不会自动重载 Python 模块，修复后须再次重启，再做真实写入、同周追加及写后检索验收，不能以新测试进程替代该最后一步。
